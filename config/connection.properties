# Local development configuration.

source.db.url=${DB_SRC_URL}
source.db.user=${DB_SRC_USER}
source.db.password=${DB_SRC_PASSWORD}
source.db.schema=dev

source.file.path=sql/ddl/schema-definition.sql
source.file.schema=dev

target.db.url=${DB_TGT_URL}
target.db.user=${DB_TGT_USER}
target.db.password=${DB_TGT_PASSWORD}
target.db.schema=test

report.path=reports/schema-diff.md

groq.api.key=${GROQ_API_KEY}
